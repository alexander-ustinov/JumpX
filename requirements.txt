# Core: frozen Qwen3.5 base + trained lenses, distillation training
# torch must provide torch.optim.Muon (used in trainer.py) -> 2.12+
torch==2.12.0
transformers==5.12.0
datasets==5.0.0

# Config, CLI, progress
omegaconf==2.3.1
fire==0.7.1
tqdm==4.67.1

# Streaming SlimPajama .jsonl.zst shards from the HF hub (data_stream.py)
huggingface_hub==1.19.0
zstandard==0.23.0
